# 一.globalFunctions 包含的全局方法
  # 1.判断图片是否加载成功或者完成 imgLoad
  # 2.倒计时  countDown
  # 3.获取当前路由 getRouterPath
  # 4.获取路由中的参数 getRouterParams
  # 5.消息提示 toMessage
  # 6.apiPost apiGet
  # 7.objToStr 将对象转变为=连接的字符串 
  # 8.getRightItem 搜索栏本地设置
  # 9.getToday 今天日期
  # 10.getLastYear 往前推算一年
  
# 二.windowSize是监听窗口尺寸变化